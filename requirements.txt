# requirements.txt — зависимости Python-проекта orm_site_prj.
#
# Установка:
#     pip install -r requirements.txt
#
# Создание virtualenv (рекомендуется):
#     python -m venv venv
#     source venv/bin/activate        # Linux / macOS
#     venv\Scripts\activate           # Windows
#     pip install -r requirements.txt
#
# Обновление этого файла после установки новых пакетов:
#     pip freeze > requirements.txt

# ---------------------------------------------------------------------------
# Веб-фреймворк
# ---------------------------------------------------------------------------

# Django 5.2 LTS — долгосрочная поддержка до апреля 2028 г.
# Документация: https://docs.djangoproject.com/en/5.2/
# Changelog: https://docs.djangoproject.com/en/5.2/releases/5.2/
Django==5.2

# ---------------------------------------------------------------------------
# Работа с часовыми поясами (требуется Django при USE_TZ=True)
# ---------------------------------------------------------------------------

# tzdata — кросс-платформенная база данных часовых поясов IANA.
# Обязательна на Windows; на Linux/macOS используется системная /usr/share/zoneinfo,
# но пакет всё равно рекомендуется для воспроизводимости окружения.
tzdata==2024.2

# ---------------------------------------------------------------------------
# Адаптер базы данных (опционально, при переходе на PostgreSQL)
# ---------------------------------------------------------------------------

# psycopg2-binary==2.9.10   # PostgreSQL-адаптер (раскомментируйте при необходимости)

# ---------------------------------------------------------------------------
# Вспомогательные инструменты разработки (dev-only)
# ---------------------------------------------------------------------------

# Раскомментируйте при разработке:
# django-debug-toolbar==4.4.6  # Панель отладки в браузере
# flake8==7.1.1                # Линтер PEP 8
# black==24.10.0               # Автоформатирование кода
